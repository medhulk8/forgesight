# ForgeSight env template. Copy to .env (gitignored). No secrets required for the
# core pipeline (all open models/data, §3.3). Only needed if the HF model is gated.

# Hugging Face token — set ONLY on Kaggle (via Kaggle Secrets), not in code.
# HF_TOKEN=
