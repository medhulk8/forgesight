__pycache__/
*.py[cod]
.pytest_cache/
.venv/
*.egg-info/
build/
dist/

# secrets
.env

# data + artifacts are large / generated (§2) — anchored to repo root so the
# src/forgesight/data/ PACKAGE is NOT ignored (unanchored `data/` matched it).
/data/
/artifacts/
